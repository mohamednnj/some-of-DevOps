name: Learn GitHub Actions
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: test workflows
        run: echo "test workflow"

      - name: check repo
        run: actions/checkout@v6.0.2