name: test scheduling
on:
  schedule:
    - cron: '40 2 * * *' # Runs every day at midnight
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: check repo
        uses: actions/checkout@v6

      - name: listing files
        run: ls -la

      - name: run tests
        run: npm test